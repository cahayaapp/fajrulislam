CAHAYA APP v138
- Professional native macOS wrapper (WKWebView), universal Apple Silicon + Intel.
- Developer ID signing + Hardened Runtime + Apple notarization + stapling workflow.
- Signed/notarized DMG only; legacy unsigned/ad-hoc installer removed.
- Mac download button refuses unsigned fallback and waits for notarized installer metadata.
